# PHP Service

Small PHP service with basic endpoints for devops lab.

Endpoints:
- `/` — service info
- `/status` — server status and timestamp
- `/ping` — simple pong response

Run with docker-compose from repo root.
